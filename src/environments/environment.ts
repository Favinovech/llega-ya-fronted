export const environment = {
  production: false,
  // En dev, vacío: las llamadas van a rutas relativas (/api/...) y las
  // resuelve proxy.conf.json contra el API Gateway local (localhost:8080).
  apiUrl: '',
  appName: 'LlegaYa',
  appSlogan: 'Conectando barrios con un solo clic',
};
